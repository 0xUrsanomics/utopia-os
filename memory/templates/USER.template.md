# USER. Deep Model of the Operator

> **Template.** Copy to `memory/USER.md` and fill in. This is who the operator IS, not what they do.
> Read it when building anything for them, when calibrating advice, when deciding what to flag, push
> back on, or let slide. The richer the model, the better the service. This file is always-loaded
> (Tier 1), so keep it dense and current, not exhaustive. Re-consolidate quarterly or after any major
> life/venture pivot.

---

## Quick reference

> A scannable fact block. The things you'd want in working memory on every reply.

- **Name / handle**: `<fill in>`
- **Location / timezone**: `<fill in, e.g. City, GMT+X>`
- **Primary venture / role**: `<fill in>`
- **Languages**: `<fill in, with honest proficiency levels. overclaims here cause real harm downstream>`
- **Contact surfaces**: `<fill in the channels the agent operates through>`

---

## Background

> Where they came from, the shape of their path, the one or two formative pivots. Not a resume. The
> narrative that explains how they decide. Example prompts: What did they do before this? What was the
> big turn? Are they a planner or a signal-follower?

`<fill in>`

---

## Current state

> What's true right now. This section goes stale fastest, so date it and keep it honest.

### Financial reality
> Month-to-month or runway-secure? What framing reads as out-of-touch vs grounded? Any numbers the
> agent should NOT anchor on (e.g. a below-market founder draw)?

`<fill in>`

### Active bets / workstreams
> The live projects. Keep these SHORT and pointer-like. Detailed live state belongs in Tier-2
> dossiers (`memory/Context/`), not here. A cached snapshot in this file WILL go stale and mislead.

`<fill in, or point to memory/Context/<dossier>.md>`

---

## Daily routine

> A typical day, hour-blocked. This lets the agent schedule discretionary work into the right window
> and avoid interrupting protected time. Prompts: When do they wake? When is deep-work vs reactive
> time? Are weekends work or rest? Any non-negotiable blocks (gym, family, commute)?

`<fill in>`

---

## Devices + accounts

> What they read on, what device heuristics exist (e.g. lowercase first letter = laptop, uppercase =
> phone-autocapitalize), which accounts are load-bearing. Informs formatting and auth decisions.

`<fill in>`

---

## Family + inner circle

> The people who matter, and the boundary rules around them. Prompt: who is off-limits for public
> content? Who is both a personal relationship AND a business stakeholder (handle with care)?

`<fill in>`

---

## Values + decision patterns

> How they decide, what they walk away from, what they optimize for. This is the highest-leverage
> section for calibrating advice. Prompts: What kind of deal/option do they reflexively reject? What
> framing resonates (compounding vs 10x)? Systems-first or revenue-first? Any hard service-line or
> ethical boundaries?

- `<value / pattern>`
- `<value / pattern>`

### Domain-shape patterns
> Recurring behavior in their core domain (deals, research, building, whatever it is).

- `<pattern>`

---

## Temperament, blind spots, triggers

> The most useful and most sensitive section. How they behave under load, where they reliably slip,
> and what breaks trust instantly.

### Strengths
- `<fill in: what they do exceptionally, e.g. high-fidelity execution under load>`

### Blind spots / patterns to watch
> Where the agent should proactively intervene because the operator reliably won't.

- `<fill in: e.g. a task class they procrastinate on unless there's a trigger event>`

### Triggers
> The things that instantly break the working relationship. Be specific, these are load-bearing.

- `<fill in: e.g. AI-slop wordlist, hospitality openers, sycophantic praise, being asked to confirm
  what was just answered>`

---

## Time / work / communication patterns

> How they work, how they ask, how they read, how they correct. Capture the actual grammar of their
> instructions so the agent parses intent correctly.

### How they ask
> e.g. terse parallel line-by-line directives, single-word kickoffs that mean "go", pre-execution
> pause words, context-drop-not-request patterns.

`<fill in>`

### How they read
> e.g. mobile-first, reads-over-listens, long docs as attachment not chat-paste.

`<fill in>`

### How they correct
> e.g. blunt but warm, no grudges, self-roasts when the pattern is theirs. Tells the agent how to
> receive a correction (one ack + the fix, not repeated apology).

`<fill in>`

---

## How to apply (when building, suggesting, replying)

> Translate the model above into concrete behavior. This is the "so what" section.

- `<fill in: e.g. frame decisions as signal-following, not 12-month roadmaps>`
- `<fill in: e.g. surface a backlog as a single list, not one-by-one across sessions>`

## What NOT to do

> The hard nevers, specific to this operator.

- `<fill in: e.g. never auto-send anything visible to others>`
- `<fill in: e.g. never lead with paid options, exhaust free/existing first>`

---

*Source: `<fill in what this file consolidates from>`. Re-consolidate quarterly OR after any major
life/venture pivot.*

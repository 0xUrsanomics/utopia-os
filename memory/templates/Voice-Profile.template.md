# Voice Profile. <OPERATOR_NAME>

> **Template.** Copy to `memory/Voice-Profile.md` and fill in over time. This is the observation
> substrate that FEEDS the SOUL.md voice mandates. SOUL is the compiled constitution (mandates);
> this is the raw dimensional evidence you accumulate by watching how the operator actually writes.
> Each dimension is a `dimension:: <name>` / `pattern:: <observation>` pair. Leave `pattern::` blank
> until you have a real observation, never fabricate one. Add new dimensions as you notice them.

category:: voice
user:: <operator-slug>

---

dimension:: sentence-length
pattern:: <observe: typical length, fragment vs full-sentence ratio, where they run long>

dimension:: punctuation
pattern:: <observe: em-dash usage, how rhythm is carried (periods/commas/line-breaks), colon habits>

dimension:: transitions
pattern:: <observe: do they use formal connectives (however, moreover) or drop them entirely>

dimension:: vocabulary
pattern:: <observe: abbreviations, slang, coined verbs, register-specific words. this dimension grows
  the largest over time. log each new token with its meaning and register>

dimension:: language-mix
pattern:: <observe: single language or code-switching. if code-switching, note which register triggers
  which language (casual vs technical vs venting)>

dimension:: tone
pattern:: <observe: blunt vs hedged, presence of softeners, directness>

dimension:: formatting
pattern:: <observe: tables vs prose, header density, list style>

dimension:: structure
pattern:: <observe: lead-with-point vs build-up, preamble habits>

dimension:: questions
pattern:: <observe: how they ask. trailing forms ("X or how?", "X right?"), soft-check vs
  opinion-exchange invitations, verification pings>

dimension:: decision-framing
pattern:: <observe: how they bucket choices. action-verb triads (BUILD/SKIP/DEFER), single-verb
  lifecycle directives (park it, kill it, ship it, lock it)>

dimension:: kickoff-trigger
pattern:: <observe: the terse phrases that flip a queued item to "go now". single words, letters,
  numbered selects, batch-approval forms>

dimension:: anti-patterns
pattern:: <observe: the words/constructions that read as NOT them. this becomes the SOUL banned-list>

---

> **How to use this file.**
> - Append observations as you catch them, dated where useful. Do not invent patterns to fill a blank.
> - When a dimension accumulates enough signal, promote the load-bearing part into a SOUL.md mandate.
> - Consider splitting a dated running journal (`Voice-Profile-Journal.md`) out of this file if the
>   raw observation log grows long, keeping this file to the compiled dimensions (always-load detox).

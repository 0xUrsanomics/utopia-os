#!/usr/bin/env bash
# Stop-hook stub (Utopia OS). Enforce your turn-completion contract here, e.g. "every turn must end
# with a user-visible reply/send" so a silent turn can't slip through. This generic version passes;
# wire your own check. Never hard-fail in a way that traps the session. See docs/security-gates.md.
exit 0
